# Glizzy
Static Photo Gallery -  Fully static (HTML + CSS only) * Horizontal scroll gallery (smooth, mobile‑friendly) * Semantic HTML (&lt;figure>, &lt;figcaption>) * Metadata‑ready (JSON file you can expand later) * Clean folder structure * Future‑proof so you can grow the site into a full portfolio or multi‑gallery system
